<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/window_background</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:popupMenuStyle">@style/DarkPopupMenu</item>
        <item name="android:dropDownListViewStyle">@style/DarkDropDown</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:fitsSystemWindows">false</item>
    </style>

    <!-- Dark dropdown style -->
    <style name="DarkPopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">#1a1f2e</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="DarkDropDown" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:background">#1a1f2e</item>
        <item name="android:divider">#2a3040</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0F2340</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <!-- Splash ပြီးရင် NoActionBar theme ကို ပြောင်းမယ် (Status bar settings တွေ ပါဖို့) -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
